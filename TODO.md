**Things I want to do:**

- Make it so that the save button calculates the expected file size. The save progress bar should then keep measuring the current file size and the percentage should be the current file size divided by the expected file size.
- Make the ROIs use the max frames calculated based on acquisition settings during acquisition, but max frames from "Preview Settings" during preview (currently they always use the preview max frames).
- Figure out how to speed up saving. Currently it takes AGES to save a file (understandably its over 2GB file but it should take like 20 seconds at my max write speed of 300MB/s).
- Add a loading bar whilst loading a large file for preview.
